{
  "name": "reinats-film",
  "private": true,
  "description": "Source for the home page film (assets/js/xp.js). Run: npm install && npm run build",
  "type": "module",
  "scripts": { "build": "node build.mjs" },
  "devDependencies": { "esbuild": "^0.28.2", "three": "^0.186.1" }
}
